# Tractyl (nice_nano + tracktyl shields) — isolated pipeline (upstream main)
# HWMv2: nice_nano defaults to rev 2.0.0 = v2 hardware.
---
include:
  - board: nice_nano//zmk
    shield: tracktyl_left
  - board: nice_nano//zmk
    shield: tracktyl_right
